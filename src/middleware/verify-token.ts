import { NextFunction, Request, Response } from "express";

export const verityToken = async (req: Request, res: Response, next: NextFunction) => {
    // pegar token
    // validar token válido
    // buscar token 
}
  
  